spring:
  security:
    client:
      kakao:
        client-id: 563d3e88ec3fdc32b209014aa932bcc3
        client-secret: XsC1DAwg6iN6bY78vkicvlKMUXeDuKt8

  config:
    import: optional:application-secret.yml

  datasource:
    url: jdbc:mysql://ocb.iptime.org:3309/MAP_MO
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
    open-in-view: false

server:
  port: 8080

logging:
  level:
    root: INFO
    com.pbl.mapmo: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql: TRACE
